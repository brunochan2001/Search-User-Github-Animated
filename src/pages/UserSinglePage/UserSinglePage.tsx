import React from 'react';
import DefaultLayout from '../../layout';
import { UserItemContent } from '../../container';

const UserSinglePage = () => {
  return (
    <DefaultLayout>
      <UserItemContent />
    </DefaultLayout>
  );
};

export default UserSinglePage;
